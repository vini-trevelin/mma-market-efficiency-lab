# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the target plan fully before starting, honor its STOP conditions, and update the status row when done.

Planned at commit `7378ca3` on branch `research-fight-model`.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add calibrated walk-forward evaluation | P0 | L | - | DONE |
| 002 | Validate the serving model artifact | P0 | M | 001 | DONE |
| 003 | Enforce swapped-input probability invariance | P1 | M | 002 | DONE |
| 004 | Vectorize card prediction serving | P1 | M | 002, 003 | DONE |
| 005 | Define generated artifact and data version policy | P0 | M | - | DONE |
| 006 | Add model cards and experiment registry | P1 | M | 001, 005 | DONE |
| 007 | Add feature ablation and stability reports | P1 | M | 001 | DONE |
| 008 | Replace pickle calibrator serialization | P2 | S | 002 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 001 should land before 002 because the model quality gate needs calibrated walk-forward metrics to enforce.
- 002 should land before new model-serving work is trusted because it makes the currently served artifact loadable and measurable.
- 003 can run after or alongside 002, but portfolio-grade probabilities require this contract before public demos.
- 004 depends on 002 and 003 because faster card serving must preserve the verified probability contract.
- 005 should happen before more generated reports/models are committed.
- 006 depends on 001 and 005 because model cards need stable metric outputs and a clear artifact policy.
- 007 depends on 001 because ablation should reuse the same fold definitions and metrics.
- 008 can run independently, but should verify against the serving checks from 002 if available.

## Repository Verification Commands

Use these commands unless a plan narrows the scope:

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Full Python tests | `uv run pytest` | exit 0, all tests pass |
| Targeted model tests | `uv run pytest tests/test_models.py tests/test_model_calibration.py` | exit 0 |
| Model benchmark smoke | `uv run python -m mma_eff_lab benchmark-fight-models` | exit 0; writes benchmark artifact |
| Calibrated model smoke | `uv run python -m mma_eff_lab train-calibrated-ufc-catboost` | exit 0; writes calibrated model artifacts |
| Quality gate | `uv run python -m mma_eff_lab validate-model-quality` | exit 0; writes model quality report |

Do not run commands that refresh external data, mutate raw caches, or commit files unless the operator explicitly asks.

## Findings Considered And Deferred

- CI workflow missing: worthwhile for portfolio polish, but deferred because the requested top eight focuses on model validity, probability serving, and artifact policy.
- Large module refactors: `apps/web/src/App.tsx`, `src/mma_eff_lab/api/app.py`, `src/mma_eff_lab/warehouse/build.py`, and `src/mma_eff_lab/audit/warehouse.py` are large enough to merit decomposition, but this is lower leverage than the probability-validation work.
- Betting/odds ingestion: important for later market-efficiency work, but intentionally deferred because the current objective is win probabilities rather than betting.
